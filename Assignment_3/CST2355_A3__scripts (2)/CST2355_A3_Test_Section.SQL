SET TERMOUT OFF

DELETE FROM SECTION WHERE SectionID > 35;
DELETE FROM PROFESSOR WHERE ProfessorID >= 6;

SET LINESIZE 200
COLUMN SectionID     FORMAT 99
COLUMN Semester      FORMAT A3
COLUMN SectionName   FORMAT A20
COLUMN CourseID      FORMAT 99
COLUMN ProfessorID   FORMAT 99
COLUMN FirstName     FORMAT A10
COLUMN LastName      FORMAT A10
COLUMN Email         FORMAT A10

SET TERMOUT ON

SELECT 'BEFORE TEST' AS X, SECTION.* FROM SECTION WHERE SectionID >= 35;
SET TERMOUT OFF

INSERT INTO V_SECTION (Semester, SectionNumber, StartDate, EndDate, CourseID, ProfessorID)
VALUES ('22F', '451', CURRENT_DATE, CURRENT_DATE, 1, null);

INSERT INTO V_SECTION (Semester, SectionNumber, StartDate, EndDate, CourseID, ProfessorID)
VALUES ('22F', '452', CURRENT_DATE, CURRENT_DATE, 1, 1);

COMMIT;

SET TERMOUT ON
SELECT 'AFTER TEST' AS X, SECTION.* FROM SECTION WHERE SectionID >= 35;

SET LONG 80